export class ViewProvider {
    static root = '../../app/';
    static viewPath = 'views';
    static home = ViewProvider.root + ViewProvider.viewPath + '/home.html';
    static main = ViewProvider.root + ViewProvider.viewPath + '/main.html';
    static app = ViewProvider.root + ViewProvider.viewPath + '/app.html';
    static card = ViewProvider.root + ViewProvider.viewPath + '/card.html';
}